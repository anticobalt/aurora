<% snip = StringConstructor.new.snippet(textfile.contents) %>

<% snip.split("\n").each do |line| %>
  <%= raw line %><br>
<% end %>
<% unless snip == textfile.contents or snip.downcase.include? "error" %>
  <%= link_to("Read more", textfile_path(textfile)) %>
<% end %>

<!DOCTYPE html>
<html>
  <head>
    <title>Aurora</title>
    <%= csrf_meta_tags %>
    <% # added extensions to 'application' to load files properly: http://stackoverflow.com/a/35965674 %>
    <%= stylesheet_link_tag    'application.css', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application.js', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <% if flash.notice %>
      <div id="alert">
        <p><%= flash.notice %></p>
      </div>
    <% end %>
    <%= yield %>
    <% # The set href prevents page from automatically moving to top %>
    <% # => since that's already handled by a JS function in application.css. %>
    <% # <a> used instead of <p> so that mouse changes on mouseover %>
    <% unless @no_scroll %>
      <a href="#" id="link_to_top">Return to Top</a>
    <% end %>
  </body>
</html>
